#pragma once
#include <map>
#include <string>

extern std::map<int, std::string> syscall_map;
extern std::map<int, std::string> open_modes;
extern std::map<int, std::string> mmap_prot;
extern std::map<int, std::string> mmap_flags;
